@using Client.Models
@using Client.Services;

@inject HttpClient Http
@inject IJSRuntime JS
@inject UserState AuthState

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Client</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        @if (AuthState.IsLoggedIn && AuthState.CurrentUser is not null && AuthState.CurrentUser!.Role == "User") //User
        {
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="/user/projects">
                    <span class="bi bi-palette2-nav-menu" aria-hidden="true"></span> My Projects
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="/logout">
                    <span class="bi bi-door-open-fill-nav-menu" aria-hidden="true"></span> Log Out
                </NavLink>
            </div>
        }
        else if (AuthState.IsLoggedIn && AuthState.CurrentUser is not null && AuthState.CurrentUser!.Role == "Admin") //Admin
        {
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="/logout">
                    <span class="bi bi-door-open-fill-nav-menu" aria-hidden="true"></span> Log Out
                </NavLink>
            </div>
        }
        else if (!AuthState.IsLoggedIn) //Anonymous
        {
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="/login">
                    <span class="bi bi-door-open-nav-menu" aria-hidden="true"></span> Log In
                </NavLink>
            </div>
        }
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;
    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

    protected override void OnInitialized() => AuthState.Changed += OnAuthChanged;

    private void OnAuthChanged() => InvokeAsync(StateHasChanged);

    public void Dispose() => AuthState.Changed -= OnAuthChanged;
}
